package com.flowershop.backendproject.Dtos.Mappers;

public class ProdusMapper {
   /* public static ProdusDto toProdusDTO(Produs produs){
        return new ProdusDto(produs.getId(), produs.getNume(), produs.getPret(), produs.getStoc(), produs.getSezon(), produs.getCategory().getId());
    }

    public static Produs toEntity(ProdusDto produsDto){
        return new Produs(produsDto.getId(), produsDto.getNume(), produsDto.getPret(), produsDto.getSezon(), produsDto.getStoc());
    }
*/
}
