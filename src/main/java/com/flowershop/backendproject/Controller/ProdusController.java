package com.flowershop.backendproject.Controller;

public class ProdusController {
    /*
    public Long insert(ProdusDto produsDto){
        Produs produs = ProdusMapper.toEntity(produsDto);
        Optional<Categorie> categorie = categorieRepository.findById(produsDto.getId_categorie()));
        produs.setCategorie(categorie.get());
        produs = produsRepository.save(produs);
        LOGGER.debug("Produsul cu id-ul {} si categoria id {} a fost inserat in baza de date.", produs.getId(), categorie.get().getId());
        return produs.getId()
    }*/
}
